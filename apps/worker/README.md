# Trend Pulse Worker

Reserved for source ingestion, normalization, scheduled intelligence jobs, trend detection and scoring workloads.

Python runtime will be introduced only when the first real source connector is selected.
